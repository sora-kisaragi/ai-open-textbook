# 教師用ガイド: 記述統計によるデータ分析

## レッスンのねらい

問いとデータ型から要約を選び、度数、割合、平均値、中央値、範囲、教材で定義した四分位範囲を計算します。分かち書き済み合成コメントは手集計してから任意のPythonで照合し、頻度と重要度、前処理と文脈損失を区別します。一つの統計量や語頻度から母集団、因果、重要意見を断定させません。

## 前提確認

D5の欠測、型、テキスト前処理履歴と、割合、四則演算を確認します。四分位数の計算法が複数あることを明示し、本教材の手順を固定します。形態素解析や第三者ライブラリは前提にしません。

## 学習者用本文との対応

1. 問いに合う要約を選ぶ。（D6.O1）
2. 8個の合成値で度数、割合、中心、散らばりを計算する。（D6.O2）
3. 4件の分かち書き済み合成文を手集計し、任意のPython結果と照合する。（D6.O1〜O2）
4. 頻度と重要度、前処理と文脈損失を区別する。（D6.O3）
5. 平均と中央値、同平均で異なる分布を比較する。（D6.O3）

## 時間配分の目安

90〜120分です。第1時に数値の選択と計算、第2時に手作業の語頻度、第3時相当の発展で分布、前処理、文脈、解釈の限界を扱えます。

## 進め方と形成的評価

D6.O1は選択理由、D6.O2は式・単位・分母と手集計表、D6.O3は同じ平均を持つ二つの分布、頻度と重要度、元文と頻度表の比較で確認します。計算機利用時も、欠測処理、並べ替え、分割・除外規則を説明させます。

## 想定される誤解

- すべての数値へ平均を使う。
- 割合の分母を書かない。
- 平均値を典型・散らばり・原因のすべてとする。
- 8回を全期間へ一般化する。
- 最頻語を最重要の意見とする。
- 前処理規則、元文、失われる文脈を報告しない。

## 支援とアクセシビリティ

式、並べ替え済み一覧、数直線、文章の複数表現を用意します。語頻度は記入済みの行別チェック表と元文を併置します。計算機や表計算の利用を認め、暗算速度ではなく、選んだ要約、式、単位、分母、前処理、解釈を評価します。

手作業が難しい場合は途中値を提示し、平均と中央値、頻度と重要度、一般化の限界を説明する課題へ置き換えられます。

## 合成データと出典上の注意

利用人数とT01〜T04のコメントは計算検証用の合成データです。統計的推測や実在施設の評価は扱いません。実在する生徒の自由記述は使わず、4件の合成文から利用者全体を推測させません。

## 練習問題

- **問いに合う要約を選ぶ**（`prob.info1.data.descriptive.analysis.001.v1`）: D6.O1
- **度数、割合、中心、散らばりを計算する**（`prob.info1.data.descriptive.analysis.002.v1`）: D6.O2
- **平均だけでは見えない分布を説明する**（`prob.info1.data.descriptive.analysis.003.v1`）: D6.O3
- **数値と合成テキストの分析結論を評価する**（`prob.info1.data.descriptive.analysis.004.v1`）: D6.O1〜O3

## 教材・準備物

- 8件の合成数値、式・単位・対象件数を残す計算票、別手段の照合票。
- 分かち書き済み合成テキストと前処理規則表。

## 期待される応答とフィードバック

| 応答 | フィードバック |
| --- | --- |
| 度数・割合・中心・範囲を式付きで求める | 対象8件、単位、別手段の一致を確認する |
| 平均だけで典型や因果を断定する | 中央値、散らばり、標本範囲へ戻す |

## フィードバックと再確認

D6.O2は答えだけでなく、度数1、割合式、合計、中央二値、最大・最小の再計算証拠で確認します。

## 補充と任意発展

補充では四件の平均・中央値・範囲だけを扱います。任意発展では同じ平均で分布が異なる二組を作ります。

## Claim-level evidence ledger

| Claim locator | Exact claim | Claim type | Evidence | Check | Scope note |
| --- | --- | --- | --- | --- | --- |
| `lesson.info1.data.descriptive.analysis.v1` > `例題: 同じデータを再計算する` | For the stated eight values, the count at or above 20 is 1, the proportion is 12.5%, the mean is 17.5, the median is 15.5, and the range is 19. | deterministic calculation | Deterministic calculations shown in the lesson: `1/8`, `140/8`, `(15+16)/2`, and `31-12`. | 2026-07-24; AI calculation review; `supported` | The values are synthetic; the calculations do not support causal or population claims. |

本文末の独立転移20分と確認5分は、2時限に含めません。

## 50分授業の到達点と判断

| 時限 | 学習者の到達点 | 50分の区切り | 続行条件 |
| --- | --- | --- | --- |
| 第1時 | 数値データの問いに合う度数・割合・中心・散らばりを選び、手計算して解釈する。 | 同じデータへ二つの要約を適用し、分布と外れ値を含めて比較して止める。 | 計算値だけで終わらず、問いに対して何を示すかを言葉で説明できる。 |
| 第2時 | テキストの語頻度を計算し、前処理・文脈損失・頻度と重要性の違いを評価する。 | 語頻度表と元文の確認結果を並べ、言えることと言えないことを書いて止める。 | 高頻度を重要性や意見の強さと同一視せず、前処理の影響を示せる。 |

## レビュー上の注意

本ガイドはドラフトであり、承認、公開、安定性、最終的な教育課程整合を示しません。
